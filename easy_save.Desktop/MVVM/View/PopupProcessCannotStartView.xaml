<Window
    x:Class="easy_save.Desktop.MVVM.View.PopupProcessCannotStartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{DynamicResource TitlePopUp}"
    MinWidth="375"
    MinHeight="150"
    MaxWidth="375"
    MaxHeight="150"
    mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <TextBlock
                Margin="5,5,0,15"
                Text="{DynamicResource ErrorPopUp}"
                TextWrapping="WrapWithOverflow" />
            <TextBlock Margin="5,5,0,0" Text="{DynamicResource ErrorPopUp2}" />
        </StackPanel>

        <Button
            Width="50"
            Height="20"
            Margin="255,70,15,15"
            Click="Button_Click"
            Content="{DynamicResource Close}" />
    </Grid>
</Window>